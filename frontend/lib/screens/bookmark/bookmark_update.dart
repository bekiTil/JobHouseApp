import 'package:flutter/material.dart';

class UpdateBookmark extends StatelessWidget {
  const UpdateBookmark({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('update bookmark')),
    );
  }
}
